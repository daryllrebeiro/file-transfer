import type { Metadata } from '../types';
const apiURL = import.meta.env.VITE_API_URL ?? 'http://localhost:8080';
export async function createTransfer(metadata: Metadata): Promise<{ id: string; url: string; expiresAt: string }> { const response = await fetch(`${apiURL}/api/transfers`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(metadata) }); if (!response.ok) throw new Error(response.status === 400 ? 'This file is too large for the configured limit.' : 'The transfer service is unavailable.'); return response.json(); }
export function socketURL(id: string): string { const configured = import.meta.env.VITE_WS_URL; if (configured) return `${configured.replace(/\/$/, '')}/ws/${id}`; return `${apiURL.replace(/^http/, 'ws')}/ws/${id}`; }